var avengers = ['Spiderman', 'AntMan', 'Hulk'];
var mutants = ['Wolverine', 'Ciclops', 'Storm'];
var defenders = ['Daredevil', 'IronFist', 'Luke Cage'];

console.log("avengers length:", avengers.length);
console.log("mutants length:", mutants.length);
console.log("defenders length:", defenders.length);

/*
let i = 0; // Variable contadora (1)
let total = 0;
 
while (i < a.length) { // Condición de parada (2)
    total += a[i];
 
    i++; // Sumador (3)
}
 
console.log(total);
 
 
let total = 0;
 
//   ----1----  -----2------ --3--
for (let i = 0; i < a.length; i++) {
    total += a[i];
}
 
console.log(total);
*/